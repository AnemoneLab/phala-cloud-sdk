/**
 * Phala Cloud SDK version
 */
export const SDK_VERSION = "0.1.0";

/**
 * Default Phala Cloud API URL
 * 注意：API端点不再需要在路径中添加 '/api/v1'，因为每个请求中已包含此前缀
 */
export const DEFAULT_CLOUD_API_URL = "https://cloud-api.phala.network";

/**
 * Default Phala Cloud Web URL
 */
export const DEFAULT_CLOUD_URL = "https://cloud.phala.network";

/**
 * Default TEE Simulator Docker image
 */
export const TEE_SIMULATOR_IMAGE = "phalanetwork/tappd-simulator:latest"; 